angular.module('loadMeApp')
	.controller("loadedController", ["$scope", function ($scope) {
		var vm = this;
		// vm.name = "test";
		// vm.email = "rthrh";
		// vm.contact = {};
		// vm.contact.email = "";
		// vm.contact.text = "";

		// TODO: need to add form validation messages
	}]);